services:
  - type: web
    name: apiss-remote-relay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: AGENT_SECRET
        sync: false
